import { key } from './key';
import { mandel } from './mandel';
import { consume } from './consume';
import { collection } from './collection';

const props = {
  key,
  mandel,
  consume,
  collection,
};

export { props };
